<UserControl x:Class="Cinema.Presentation.Wpf.Controls.MainFilmControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cinema.Presentation.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="7*" />
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding Movies}">
        </ListBox>
        <Button Grid.Row="2" Command="{Binding AddCommand}">Add Movie</Button>
    </Grid>
</UserControl>
